package algosAndStructs;

public class BigONotation {

    //tea time complexity=  O(n)    number of sugars = n    total # of steps = 2n+2     linear time complexity
    // the 2 of 2n and +2 both remain const, so dont facotr into time complexity.   n determines growth rate

    //O(1) = constant    O(logn) logarithmic(base 2 not 10)     O(n) linear    O(nlogn) = log-star n      O(n^2) quadratic
}
